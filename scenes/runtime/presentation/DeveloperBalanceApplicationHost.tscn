[gd_scene load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/presentation/developer_balance_application_host.gd" id="1_host"]

[node name="DeveloperBalanceApplicationHost" type="Node"]
script = ExtResource("1_host")
editor_description = "Optional application-composition host for the developer balance panel. It mounts one panel and binds the existing presentation target without owning diagnostics or gameplay state."
