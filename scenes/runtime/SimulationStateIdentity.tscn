[gd_scene load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/runtime/simulation_state_identity.gd" id="1_identity"]

[node name="SimulationStateIdentity" type="Node"]
script = ExtResource("1_identity")
editor_description = "Builds internal deterministic identities from explicitly supplied pure-data simulation projections. It owns no world state and is never a presentation source."
