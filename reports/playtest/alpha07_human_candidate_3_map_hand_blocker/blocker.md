# Candidate 3 map-pointer and hand-readability blocker

This is append-only evidence for a new human retest result. Candidate 3's
earlier card-table flow record remains intact and is not renamed, overwritten,
or downgraded.

The new disposition is:

```text
PRE_GOLDEN_MAP_POINTER_AND_CARD_READABILITY_BLOCKED
AUTOMATED_READINESS_CONTRACT_GAP=true
HUMAN_GREEN=false
STEP13=PENDING
```

The six registered blockers are:

- The collapsed public drawer still occludes the map center.
- The collapsed drawer or an overlay intercepts map pointer input.
- The collapsed handle is not a compact left-edge icon.
- General and/or commodity hands are not visible in the real main table.
- Region target selection is unreachable through the real pointer path.
- Essential card purpose requires Hover instead of being readable on the card face.

The prior `400/400` probe proved the existing data and presentation flow, but
did not prove the missing top-level pointer, actual collapsed hit-rect, hand
visibility, no-Hover semantic, or real region-target dimensions. Those gaps
are now explicit acceptance dimensions for Candidate 4.

At freeze time the old headed Godot window was already closed (only a windowless
headless process remained), so this record deliberately contains no fabricated
screen or pointer evidence. A fresh real `main.tscn` session must be started
after the incremental repair.
