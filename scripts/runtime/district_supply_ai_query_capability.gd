extends RefCounted
class_name DistrictSupplyAiQueryCapability

# Opaque, identity-checked capability. It intentionally carries no player data
# and is rotated whenever the production composition rewires a session.
