# Economy application-flow privacy review

Status: `ECONOMY_APPLICATION_FLOW_PRIVACY_GREEN`

The source contract is split into `public_player_summaries` and `own_private_economy`. Private facts require an authorized local viewer and exact viewer/subject equality. A mismatch, missing dependency or uninitialized catalog returns the empty presentation.

Public output contains market facts, facilities with only explicitly public ownership, regional GDP totals and integrity, cached routes, weather, monster pressure, anonymous warehouse risk and public log clues. Own output contains exact cash, commodity GDP, allow-listed sale receipts, own facilities and own warehouse quantities.

Opponent exact cash, deltas, ledger, income paths, GDP breakdown, warehouse inventory, hands, discards, hidden owner truth, anonymous actor truth, private intel and AI metadata are absent. Session completion never widens the scope. Snapshot and rendered label/tooltip tests reject opponent sentinels.

Privacy violation count: `0`.
