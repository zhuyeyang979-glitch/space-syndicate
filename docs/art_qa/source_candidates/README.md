# Source candidate QA

`contact_sheet.png` is generated only after real approved source models have
been indexed and rendered through `RolePortraitRenderRig.tscn`.

The sheet must show each model's internal asset ID, pack, model name, and a
uniform camera preview. No placeholder contact sheet is accepted as proof of a
download or model selection.
