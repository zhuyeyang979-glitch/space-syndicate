# V0.7.1 Detached Semantic Registry

```text
CONSTITUTION_ID=space_syndicate.v071.complete
RULESET_ID=v0.7.1
BALANCE_PROFILE_ID=V071_CANDIDATE_A_FAST
PRODUCTION_RUNTIME_CONNECTION_COUNT=0
V06_MUTATION_COUNT=0
DUAL_WRITE_COUNT=0
```

This registry supersedes the V0.7 semantic registry for target-development
work without overwriting that historical file. It pins the five detached Core
domains to their V0.7.1 state and interface versions, the approved profile, the
new Save/RNG contracts, and the canonical adapter manifest.

The registry is executable reference metadata, not production wiring. Current
runtime remains V0.6. Human fun remains unproven and the first sample still has
to test maintenance pressure, replacement comprehension, commodity timing,
fizzle fairness, lead inference, overflow, multiplayer fatigue, card ratio,
lead cadence, and the Victory-pending tail.
